if (window.scrollY > 100) {
                header.style.background = 'rgba(0, 0, 0, 0.99)';
                header.style.borderBottom = '1px solid rgba(139, 115, 85, 0.4)';
            <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elite Gaming Hub - Level Up Your Game</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Rajdhani:wght@300;400;600;700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Rajdhani', sans-serif;
            background: #000000;
            color: #d4af37;
            overflow-x: hidden;
            line-height: 1.6;
        }
        
        /* Animated background */
        .bg-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            background: radial-gradient(circle at 20% 80%, #1a1a0f 0%, transparent 70%), 
                        radial-gradient(circle at 80% 20%, #0f0f0f 0%, transparent 70%), 
                        radial-gradient(circle at 40% 40%, #1f1a0f 0%, transparent 70%);
            animation: backgroundShift 15s ease-in-out infinite;
            opacity: 0.3;
        }
        
        @keyframes backgroundShift {
            0%, 100% { transform: scale(1) rotate(0deg); }
            50% { transform: scale(1.05) rotate(90deg); }
        }
        
        /* Particle system */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            pointer-events: none;
        }
        
        .particle {
            position: absolute;
            width: 1px;
            height: 1px;
            background: #8b7355;
            border-radius: 50%;
            animation: float 8s ease-in-out infinite;
            box-shadow: 0 0 2px #8b7355;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px) translateX(0px); opacity: 0; }
            50% { opacity: 1; }
        }
        
        /* Header */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 20px 0;
            background: rgba(0, 0, 0, 0.98);
            backdrop-filter: blur(15px);
            border-bottom: 1px solid rgba(212, 175, 55, 0.15);
            z-index: 1000;
            transition: all 0.3s ease;
        }
        
        nav {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 40px;
        }
        
        .logo {
            font-family: 'Orbitron', monospace;
            font-size: 2rem;
            font-weight: 900;
            background: linear-gradient(45deg, #d4af37, #8b7355, #cd853f);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: glow 3s ease-in-out infinite alternate;
            text-shadow: 0 0 10px rgba(212, 175, 55, 0.3);
        }
        
        @keyframes glow {
            from { filter: drop-shadow(0 0 5px #8b7355); }
            to { filter: drop-shadow(0 0 15px #d4af37); }
        }
        
        .nav-links {
            display: flex;
            list-style: none;
            gap: 40px;
        }
        
        .nav-links a {
            color: #d4af37;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            position: relative;
        }
        
        .nav-links a::before {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, #d4af37, #8b7355);
            transition: width 0.3s ease;
        }
        
        .nav-links a:hover {
            color: #cd853f;
            transform: translateY(-2px);
            text-shadow: 0 0 10px rgba(212, 175, 55, 0.5);
        }
        
        .nav-links a:hover::before {
            width: 100%;
        }
        
        /* Hero Section */
        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            background: linear-gradient(135deg, rgba(0, 0, 0, 0.8) 0%, rgba(26, 26, 15, 0.3) 50%, rgba(15, 15, 15, 0.8) 100%);
        }
        
        .hero-content {
            text-align: center;
            z-index: 10;
            max-width: 800px;
            padding: 0 20px;
        }
        
        .hero h1 {
            font-family: 'Orbitron', monospace;
            font-size: 4.5rem;
            font-weight: 900;
            margin-bottom: 20px;
            background: linear-gradient(45deg, #d4af37, #cd853f, #8b7355);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: titlePulse 4s ease-in-out infinite;
            text-shadow: 0 0 20px rgba(212, 175, 55, 0.3);
        }
        
        @keyframes titlePulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.03); }
        }
        
        .hero p {
            font-size: 1.4rem;
            margin-bottom: 40px;
            color: rgba(212, 175, 55, 0.7);
            font-weight: 400;
        }
        
        .cta-button {
            display: inline-block;
            padding: 18px 40px;
            background: linear-gradient(45deg, #8b7355, #d4af37);
            color: #000000;
            text-decoration: none;
            font-weight: 700;
            font-size: 1.2rem;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(139, 115, 85, 0.4);
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(212, 175, 55, 0.3);
        }
        
        .cta-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(212, 175, 55, 0.3), transparent);
            transition: left 0.5s;
        }
        
        .cta-button:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(139, 115, 85, 0.6);
            background: linear-gradient(45deg, #d4af37, #cd853f);
        }
        
        .cta-button:hover::before {
            left: 100%;
        }
        
        /* Gaming Cards Section */
        .gaming-section {
            padding: 120px 40px;
            max-width: 1400px;
            margin: 0 auto;
        }
        
        .section-title {
            font-family: 'Orbitron', monospace;
            font-size: 3rem;
            text-align: center;
            margin-bottom: 80px;
            background: linear-gradient(45deg, #d4af37, #8b7355);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: 0 0 20px rgba(212, 175, 55, 0.2);
        }
        
        .game-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
            gap: 40px;
            margin-bottom: 80px;
        }
        
        .game-card {
            background: linear-gradient(135deg, rgba(0, 0, 0, 0.9), rgba(26, 26, 15, 0.6));
            border: 1px solid rgba(139, 115, 85, 0.3);
            border-radius: 12px;
            padding: 40px;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
            backdrop-filter: blur(10px);
        }
        
        .game-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(139, 115, 85, 0.1), transparent);
            transition: left 0.6s;
        }
        
        .game-card:hover {
            transform: translateY(-10px);
            border-color: #8b7355;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.8);
            background: linear-gradient(135deg, rgba(26, 26, 15, 0.9), rgba(0, 0, 0, 0.7));
        }
        
        .game-card:hover::before {
            left: 100%;
        }
        
        .game-card h3 {
            font-family: 'Orbitron', monospace;
            font-size: 1.8rem;
            margin-bottom: 20px;
            color: #d4af37;
            text-shadow: 0 0 10px rgba(212, 175, 55, 0.3);
        }
        
        .game-card p {
            font-size: 1.1rem;
            color: rgba(212, 175, 55, 0.7);
            line-height: 1.6;
        }
        
        /* Stats Section */
        .stats-section {
            background: linear-gradient(135deg, rgba(0, 0, 0, 0.95), rgba(26, 26, 15, 0.8));
            padding: 100px 40px;
            text-align: center;
            border-top: 1px solid rgba(139, 115, 85, 0.2);
            border-bottom: 1px solid rgba(139, 115, 85, 0.2);
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 60px;
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .stat-item {
            padding: 40px 20px;
        }
        
        .stat-number {
            font-family: 'Orbitron', monospace;
            font-size: 3.5rem;
            font-weight: 900;
            color: #d4af37;
            display: block;
            margin-bottom: 10px;
            animation: countUp 2s ease-out;
            text-shadow: 0 0 15px rgba(212, 175, 55, 0.4);
        }
        
        .stat-label {
            font-size: 1.2rem;
            color: rgba(212, 175, 55, 0.7);
            font-weight: 600;
        }
        
        /* Footer */
        footer {
            background: #000000;
            padding: 60px 40px 40px;
            text-align: center;
            border-top: 1px solid rgba(139, 115, 85, 0.2);
        }
        
        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .social-links {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-bottom: 40px;
        }
        
        .social-links a {
            width: 50px;
            height: 50px;
            background: linear-gradient(45deg, #8b7355, #d4af37);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            text-decoration: none;
            color: #000000;
            font-weight: bold;
            transition: all 0.3s ease;
            border: 1px solid rgba(139, 115, 85, 0.3);
        }
        
        .social-links a:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 10px 20px rgba(139, 115, 85, 0.4);
            background: linear-gradient(45deg, #d4af37, #cd853f);
        }
        
        /* Responsive Design */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 3rem;
            }
            
            .nav-links {
                display: none;
            }
            
            .game-grid {
                grid-template-columns: 1fr;
            }
            
            .section-title {
                font-size: 2.2rem;
            }
            
            nav {
                padding: 0 20px;
            }
            
            .gaming-section {
                padding: 80px 20px;
            }
        }
    </style>
</head>
<body>
    <!-- Animated Background -->
    <div class="bg-animation"></div>
    
    <!-- Particles -->
    <div class="particles" id="particles"></div>
    
    <!-- Header -->
    <header>
        <nav>
            <div class="logo">SHADOW REALM</div>
            <ul class="nav-links">
                <li><a href="#home">Home</a></li>
                <li><a href="#games">Games</a></li>
                <li><a href="#tournaments">Tournaments</a></li>
                <li><a href="#community">Community</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </nav>
    </header>
    
    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="hero-content">
            <h1>FORGE YOUR DESTINY</h1>
            <p>Enter the realm where legends are born in darkness and heroes rise from ashes</p>
            <a href="#games" class="cta-button">START PLAYING</a>
        </div>
    </section>
    
    <!-- Gaming Section -->
    <section class="gaming-section" id="games">
        <h2 class="section-title">FEATURED GAMES</h2>
        <div class="game-grid">
            <div class="game-card">
                <h3>SHADOW'S LEGACY</h3>
                <p>Journey through ancient realms where darkness and light collide. Master forbidden arts and uncover the secrets of a world torn between order and chaos.</p>
            </div>
            <div class="game-card">
                <h3>CRIMSON FRONTIER</h3>
                <p>Survive the untamed wilderness in this brutal adventure where every choice matters. Hunt legendary beasts and carve your destiny in blood-soaked lands.</p>
            </div>
            <div class="game-card">
                <h3>THRONE OF ASHES</h3>
                <p>Rise from the ruins of fallen kingdoms in this dark fantasy epic. Command armies, forge alliances, and claim your rightful place among the lords of war.</p>
            </div>
            <div class="game-card">
                <h3>FORGOTTEN REALM</h3>
                <p>Explore vast, mysterious landscapes filled with ancient magic and deadly secrets. Uncover the truth behind a civilization lost to time and shadow.</p>
            </div>
        </div>
    </section>
    
    <!-- Stats Section -->
    <section class="stats-section">
        <h2 class="section-title">GAMING STATISTICS</h2>
        <div class="stats-grid">
            <div class="stat-item">
                <span class="stat-number">2.5M+</span>
                <span class="stat-label">Active Players</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">150K</span>
                <span class="stat-label">Tournaments</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">$50M</span>
                <span class="stat-label">Prize Pool</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">24/7</span>
                <span class="stat-label">Gaming Action</span>
            </div>
        </div>
    </section>
    
    <!-- Footer -->
    <footer>
        <div class="footer-content">
            <div class="social-links">
                <a href="#">TW</a>
                <a href="#">FB</a>
                <a href="#">IG</a>
                <a href="#">YT</a>
                <a href="#">DC</a>
            </div>
            <p>&copy; 2025 Elite Gaming Hub. All rights reserved. Level up your legend.</p>
        </div>
    </footer>
    
    <script>
        // Particle system
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            const particleCount = 50;
            
            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 6 + 's';
                particle.style.animationDuration = (Math.random() * 4 + 4) + 's';
                
                // Random colors for particles
                const colors = ['#8b7355', '#d4af37', '#654321', '#b8860b'];
                particle.style.background = colors[Math.floor(Math.random() * colors.length)];
                
                particlesContainer.appendChild(particle);
            }
        }
        
        // Smooth scrolling for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
        
        // Header scroll effect
        window.addEventListener('scroll', () => {
            const header = document.querySelector('header');
            if (window.scrollY > 100) {
                header.style.background = 'rgba(5, 5, 5, 0.98)';
                header.style.borderBottom = '1px solid rgba(0, 255, 136, 0.4)';
            } else {
                header.style.background = 'rgba(10, 10, 10, 0.95)';
                header.style.borderBottom = '1px solid rgba(0, 255, 136, 0.2)';
            }
        });
        
        // Animation on scroll for game cards
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };
        
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);
        
        // Observe game cards
        document.querySelectorAll('.game-card').forEach(card => {
            card.style.opacity = '0';
            card.style.transform = 'translateY(50px)';
            card.style.transition = 'all 0.6s ease';
            observer.observe(card);
        });
        
        // Initialize particles when page loads
        window.addEventListener('load', createParticles);
        
        // Add dynamic glow effect to cards on mouse move
        document.querySelectorAll('.game-card').forEach(card => {
            card.addEventListener('mousemove', (e) => {
                const rect = card.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                
                card.style.setProperty('--mouse-x', x + 'px');
                card.style.setProperty('--mouse-y', y + 'px');
            });
        });
    </script>
</body>
</html>
